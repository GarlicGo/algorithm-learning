import React from 'react';
import { Container } from './styles';
import { Car } from './Car';

export const TransitionTest: React.FC = () => {
  return (
    <Container>
      <Car />
    </Container>
  );
};
